#include "Player.h"


void Player::update() {
    // read from event queue, or pass in the event from the main game loop
}

void Player::render(const Renderer &renderer) const {
    // render to screen based on x_, y_ pixel coordinates
}
